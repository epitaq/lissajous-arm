
class Calculation :
    def __init__ (self):
        pass

    def azimuthalAngle(self, x: float, y: float) -> float:
        return azimuthal_angle

    def getFocalLength(self, picture_coordinates: list[float], polar_angle: float, distance_target_camera: float) -> float:
        return focal_length
    
    def getPolarAngleFromFocalLength(self, picture_coordinates: list[float], focal_length: float) -> float:
        return polar_angle